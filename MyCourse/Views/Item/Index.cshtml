@model IEnumerable<MyCourse.Models.Item>
@{
    ViewData["Title"] = "Index";
}

    
    <a asp-action="Create" asp-controller="Item" class="add-btn btn btn-primary"> დამატება </a>
    <form asp-action="Index" asp-controller="Item" method="get">
        <div class="searchitms d-grid mt-2"> 
            <input type="text" name="searchphrase" placeholder="ჩაწერე საძიებო სიტყვა" style="width: 30%;" />
            <input type="submit" value = "ძებნა" class="btn btn-success mt-2" />
        </div>
    </form>
    
    
 @*<table class="table table-bordered table-striped table-hover" style="margin-top: 5px;">
    <thead>
        <tr>
            <td> ბრენდი </td>
            <td> მოდელი </td>
            <td> საწვავის ტიპი </td>
            <td> საჭე </td>
            <td> მოქმედება </td>
        </tr>
    </thead>
    <tbody>
            @if( Model.Count() > 0 )
            {
                       @foreach(var itm in Model)
                {
                    <tr>
                        <td> @itm.Category.category </td>
                        <td> @itm.borrower </td>
                        <td> @itm.lender </td>
                        <td> @itm.itemname </td>
                        <td>
                             <a  class="btn btn-primary" href="/Item/Edit/@itm.id">რედაქტირება</a> |
                             <a  class="btn btn-danger" asp-controller="Item" asp-action="Delete" asp-route-id="@itm.id"> წაშლა </a> |
                             <a  class="btn btn-secondary" asp-controller="Item" asp-action="Details" asp-route-id="@itm.id">დეტალები</a> 
                         </td>
                    </tr>
                }
            } else
            {
                <tr>
                    <td colspan="5"> ინფორმაცია არ არის </td>    
                </tr>
            }
    </tbody>
</table>*@

@if( Model.Count() > 0 )
            {
                       @foreach(var itm in Model)
                {
                    <div class="shmard card w-70" style="margin-top: 10px; background-color: #83ac3c; height: 500px;">
                        <p class="carinfo"> @itm.Category.category </p>
                        <p class="carinfo"> @itm.borrower </p>
                        <p class="carinfo"> @itm.lender </p>
                        <p class="carinfo"> @itm.itemname </p>
                        <div class="cruds">
                             <a  class="btn btn-primary" href="/Item/Edit/@itm.id">რედაქტირება</a> 
                             <a  class="btn btn-danger" asp-controller="Item" asp-action="Delete" asp-route-id="@itm.id"> წაშლა </a> 
                             <a  class="btn btn-secondary" asp-controller="Item" asp-action="Details" asp-route-id="@itm.id">დეტალები</a> 
                         </div>
                    </div>
                }
            }











